<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-zinc-900 text-white flex items-center justify-center min-h-screen"
  >
    <div class="w-full max-w-md p-8 bg-zinc-800 rounded-xl shadow-lg">
      <h3 class="text-3xl font-bold mb-5 text-center">Create Account</h3>

      <form action="/register" method="post" class="flex flex-col gap-4">
        <input
          type="text"
          name="name"
          placeholder="Name"
          class="px-3 py-2 rounded-md outline-none bg-zinc-700 border border-zinc-600 focus:ring-2 focus:ring-blue-500"
        />
        <input
          type="text"
          name="username"
          placeholder="Username"
          class="px-3 py-2 rounded-md outline-none bg-zinc-700 border border-zinc-600 focus:ring-2 focus:ring-blue-500"
        />
        <input
          type="password"
          name="password"
          placeholder="Password"
          class="px-3 py-2 rounded-md outline-none bg-zinc-700 border border-zinc-600 focus:ring-2 focus:ring-blue-500"
        />
        <input
          type="email"
          name="email"
          placeholder="Email"
          class="px-3 py-2 rounded-md outline-none bg-zinc-700 border border-zinc-600 focus:ring-2 focus:ring-blue-500"
        />
        <input
          type="number"
          name="age"
          placeholder="Age"
          class="px-3 py-2 rounded-md outline-none bg-zinc-700 border border-zinc-600 focus:ring-2 focus:ring-blue-500"
        />
        <input
          type="submit"
          value="Create Account"
          class="px-3 py-2 rounded-md bg-blue-600 hover:bg-blue-700 font-semibold cursor-pointer transition duration-200"
        />
      </form>
    </div>
  </body>
</html>
